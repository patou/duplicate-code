import { codeTheParticule } from "./codeTheParticule";

codeTheParticule(color(0, 0, 255), 3, 10, 500); // pour l'animation de pluie
codeTheParticule(color(255), 1, 5, 300); // pour l'animation de neige









function color(r: number, g?: number, b?: number, a?: number){} // r, g, b, a sont des nombres entre 0 et 255 